{
  "name": "kspotlight",
  "version": "1.0.0",
  "description": "A simple HTML/JS/CSS starter template",
  "main": "main.js",
  "scripts": {
    "start": "npx serve .",
    "build:runtime-config": "node scripts/generate-firebase-runtime-config.js",
    "build:pages": "npm run build:runtime-config",
    "migrate:card-content-v2": "node scripts/migrate-card-content-v2.js",
    "build:seo": "node scripts/generate-static-seo-pages.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1",
    "firebase-admin": "^13.6.1"
  }
}
